# Deploy WordPress
name: WordPress Deployment

on:
  push:
    branches: [ dev, main ]
  pull_request:
    branches: [ main ]

jobs:
  # งานสำหรับทดสอบเมื่อมีการ Push ไปยัง dev
  test:
    runs-on: self-hosted
    if: github.ref == 'refs/heads/dev' && github.event_name == 'push'
    steps:
    # ขั้นตอนการดึงโค้ดจาก Repository
    - uses: actions/checkout@v3
    
    # ขั้นตอนการตั้งค่าสภาพแวดล้อม
    - name: Set up environment
      run: |
        cp .env.example .env
        
    # ขั้นตอนการ Deploy WordPress ด้วย Docker Compose สำหรับการทดสอบ
    - name: Deploy WordPress with Docker Compose for testing
      run: |
        docker-compose down
        docker-compose up -d wordpress-test db phpmyadmin
        
    # ขั้นตอนการตรวจสอบว่าระบบทำงานได้ปกติ
    - name: Health check
      run: |
        echo "Waiting for WordPress to start..."
        # Increase wait time to 60 seconds
        sleep 60
        
        # Implement retry logic with correct port and header inspection
        MAX_RETRIES=5
        RETRY_COUNT=0
        
        until curl -i http://wp-test:80/wp-login.php | grep -E "(WordPress|302 Found)" || [ $RETRY_COUNT -eq $MAX_RETRIES ]; do
          echo "WordPress not ready yet, retrying... ($(($RETRY_COUNT+1))/$MAX_RETRIES)"
          RETRY_COUNT=$((RETRY_COUNT+1))
          sleep 15
        done
        
        if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
          echo "WordPress health check failed after $MAX_RETRIES attempts"
          curl -i http://wp-test:80 > response.html
          echo "Response content:"
          cat response.html
          exit 1
        else
          echo "WordPress is up and running!"
        fi
      shell: /bin/bash -e {0}
    
    # ขั้นตอนการทดสอบ WordPress เบื้องต้น
    - name: Basic WordPress Tests
      run: |
        echo "🔍 เริ่มการทดสอบ WordPress เบื้องต้น"
        # ทดสอบการเข้าถึงหน้า login
        echo "ทดสอบการเข้าถึงหน้า login..."
        curl -i http://wp-test:80/wp-login.php | grep -q -E "(WordPress|302 Found)" && echo "✅ เข้าถึงหน้า login ได้" || echo "❌ ไม่สามารถเข้าถึงหน้า login ได้"
        
        # ทดสอบการเข้าถึง REST API
        echo "ทดสอบการเข้าถึง REST API..."
        curl -i http://wp-test:80/wp-json/ | grep -q "routes" && echo "✅ REST API ทำงานปกติ" || echo "❌ REST API ไม่ทำงาน"
        
        # ตรวจสอบรุ่นของ WordPress
        echo "ตรวจสอบรุ่นของ WordPress..."
        VERSION=$(curl -s http://wp-test:80 | grep -o "content=\"WordPress [0-9.]*\"" | grep -o "[0-9.]*" || echo "ไม่พบข้อมูลรุ่น")
        echo "📊 WordPress เวอร์ชัน: $VERSION"
        
        echo "🏁 การทดสอบเสร็จสิ้น"

  # งานสำหรับการ Deploy เมื่อมีการสร้าง Pull Request ไปยัง main
  deploy:
    runs-on: self-hosted
    if: github.event_name == 'pull_request' && github.base_ref == 'main'
    steps:
    # ขั้นตอนการดึงโค้ดจาก Repository
    - uses: actions/checkout@v3
    
    # ขั้นตอนการตั้งค่าสภาพแวดล้อม
    - name: Set up environment
      run: |
        cp .env.example .env
        
    # ขั้นตอนการ Deploy WordPress ด้วย Docker Compose ไปยัง Production
    - name: Deploy WordPress with Docker Compose
      run: |
        docker-compose down
        docker-compose up -d wordpress-prod db phpmyadmin

